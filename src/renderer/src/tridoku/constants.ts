/** Target maximum givens before stopping clue removal. */
export const TARGET_GIVENS_HARD = 26

/** Max random removal attempts per round while digging. */
export const DIG_ATTEMPTS_PER_ROUND = 200

/** Padding (px) around the fitted triangular board on the canvas. */
export const BOARD_PAD = 12
